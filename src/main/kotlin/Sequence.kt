/**
 * sequence
 * @param name name of sequence
 * @param sequence sequence string
 */
class Sequence(val name: String, val sequence: String) {

    /** length of sequence */
    val length = sequence.length
}